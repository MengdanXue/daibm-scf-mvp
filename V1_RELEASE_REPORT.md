# DAIBM-SCF v1.0.0 发布报告 / Release report

**daibm-scf-mvp v1.0.0 —— 供应链金融 AI 风控平台原型。** 本报告对应 Phase 5（Product Release & Tenant Isolation
Completion）：完成租户隔离闭环、演示数据、性能基线、最终安全检查、发布文档与最终验收，并冻结核心架构。

本阶段没有新增算法或模型，没有重构架构，没有引入 Kafka、Kubernetes 或其他基础设施，没有改动 Fabric 核心，
也没有修改历史审计数据。

---

## 1. 产品定位

面向供应链金融的风控平台原型，服务六类角色：
- 供应商、核心企业（企业用户）；
- 放款机构的融资方与风险经理；
- 审计员；
- 平台管理员。

平台把以下环节放在同一个可审计的系统里：

> 融资申请 → 风险评估 → 模型决策 → 放款 → 还款 → 逾期 → 预警 → 任务处理 → 结果反馈 → 模型治理

几个核心设计：
- **透明评分 + 校准：** 风险评分是可解释的透明基线，再用各放款机构自己的历史结果训练 Platt 校准模型修正。
- **模型受治理：** 模型只在独立的时间外验证通过后才能激活，可审批、可回滚。
- **全程留痕：** 每一步都写入哈希链账本，并可检测、恢复篡改。
- **数据隔离：** 数据按组织隔离，平台可同时服务多家放款机构。

定位边界：这是研究与演示用的产品化原型，而不是生产银行系统。放款与还款是受控模拟，不发生真实资金划转。
演示数据是合成数据。

## 2. 架构图

```text
                       ┌─────────────────── 浏览器（俄/中双语单页 UI）───────────────────┐
                       │ 业务工作台 · 融资生命周期 · 风险驾驶舱/预警/任务/规则 · 模型中心 │
                       │ 结果治理 · 数据快照 · 审计账本 · 平台管理（组织/配置/安全/运维） │
                       └───────────────────────────────┬─────────────────────────────────┘
                                                       │ Cookie 会话（HttpOnly, SameSite, 可选 Secure）
┌──────────────────────────────── FastAPI 单体应用（Docker, restart: unless-stopped）──────────────────────────────┐
│ 中间件：请求指标 · 403 → PERMISSION_DENIED 安全事件                                                            │
│ IdentityService（登录锁定/会话超时/密码策略）  PermissionService（角色 × 组织范围 × 资源归属，唯一权限入口）   │
│ WorkflowService（申请, 放款机构绑定）  FacilityService（生命周期状态机）  RiskInsight/Alert/Task/RuleService     │
│ OutcomeService + EligibilityService（复核/纠正/快照） ModelRegistryService（DRAFT→CANDIDATE→ACTIVE, 按组织）      │
│ AdaptiveRiskInference（仅用放款机构自己的 ACTIVE 模型）  ConfigService  OpsService（health/metrics）  Backup   │
│ 进程内后台：校准训练 worker（租约）· 风险规则监控（可配置间隔）· 心跳                                            │
│ 研究核心（离线训练的 TGNN/XGBoost，ONNX 推理，平台级、不含租户数据）                                            │
└───────────────────────────────────────────────┬────────────────────────────────────────────────────────────────┘
                                                │ SQLAlchemy 2 / psycopg 3
┌──────────────────────────────── PostgreSQL 17（Alembic 单一迁移链，head 20260930_0021）────────────────────────┐
│ 触发器：状态机守卫 · 不可变历史 · 资金守恒 · 转移审计 · 模型状态转移审计 · 租户血缘（归属填充与一致性校验）      │
│ 哈希链账本 ledger_events · security_events · system_config（版本化）· audit_grants                             │
└────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
      可选 advanced/：Hyperledger Fabric 哈希锚定（outbox） · ZKP 发票上限证明服务（Circom/Groth16）
```

## 3. 功能清单

| 领域 | 功能 |
|---|---|
| 申请 | 供应商创建申请并选择核心企业与放款机构；核心企业确认交易（应付上限、重复发票拦截、可选 ZKP）；放款机构依次完成风险评估、决策、控制措施与审计复核 |
| 融资生命周期 | 放款、分期还款与确认、逾期、处置、重组、违约、追偿、核销、结清；精确到分，每一步有版本与审计 |
| 风险评估与模型 | 透明基线评分；按组织、按范围的 Platt 校准；决策记录模型版本、工件哈希与数据快照（决策血缘） |
| 模型治理 | 模型版本注册表；独立的时间外验证门槛；自动激活或人工审批（CANDIDATE）；回滚；退役；Scope 兼容矩阵 |
| 结果治理 | 业务结果录入、复核（自动/人工）、排除与恢复、替代修订、训练资格预览、不可变数据快照 |
| 风险运营 | 风险驾驶舱、版本化风险规则（可回滚）、自动检测、预警（指派→处理→解决→审核关闭）、任务（备注、结果文件、完成） |
| 审计 | 哈希链账本、完整性检测与按事件恢复、安全事件、管理员操作审计 |
| 多租户与权限 | 组织、用户、审计授权；统一的 PermissionService；数据库层租户血缘约束 |
| 安全 | 无固定密码与硬编码密钥；登录失败锁定；空闲超时与绝对有效期；安全 Cookie；密码策略 |
| 运维 | 健康检查（应用、数据库版本、worker 心跳）；业务与系统指标（JSON 与 Prometheus 格式）；哈希校验的备份恢复；配置中心；Docker 健康检查与自动重启 |
| 演示 | 首次启动通过业务服务生成的稳定演示数据集 |

## 4. 数据隔离设计

**所有权链**：

```text
申请.lender_organization_id ──(提交后固定)──> 融资.organization_id ──> 预警/任务/业务结果
业务结果 ──> 训练任务.organization_id ──> 数据快照.organization_id ──> 校准运行.organization_id ──> 模型版本.organization_id
决策记录.assessed_by(放款机构用户) ──只能引用──> 同组织的模型版本 / 校准运行
```

**Phase 5.1 补齐的隔离**：
- **申请阶段：**
  - 申请在创建时绑定放款机构，提交后数据库禁止更改。
  - 融资方与风险经理只看到并处理本机构的申请。此前按流程阶段可见，任意银行都能看到。
  - 只有放款机构可以开立融资，服务层和数据库触发器双重校验。此前任何融资方都能对任意已批准申请开立融资。
  - 决策血缘按申请的放款机构检查访问权限。此前尚未放款的申请，其决策血缘可以跨组织读取。
- **模型注册表：**
  - 模型版本带 `organization_id`，每个组织、每个范围各有一个 ACTIVE（唯一索引为 `(organization_id, scope)`）。
  - 组织只能看到、激活、回滚本组织的模型（审计员按授权，管理员可看全部）。
  - 风险决策只使用放款机构自己的 ACTIVE 模型；没有放款机构时不使用任何校准模型。
- **数据快照：**
  - 快照带 `organization_id`，训练总体只包含本组织的业务结果。
  - 模型版本与快照、校准运行与训练任务必须属于同一组织。

**数据库强制**（迁移 `20260930_0021`）：
- 流水线四张表的归属由血缘自动填充，写入时校验一致，之后不可修改。
- 快照明细、训练观测、决策记录和业务结果如果引用其他组织的结果、模型或快照，一律拒绝。
- 即使服务层存在遗漏，数据库也会拒绝跨组织的数据血缘。

**迁移**：
- 按血缘回填归属；历史行无法唯一归属时拒绝升级。
- 回填期间暂停流水线表的治理触发器，只写归属列，不产生任何状态变化，也不改写历史。
- 在真实演示库上验证：状态、转移次数与账本链头哈希都未变化。

**平台级共享**：研究核心（在公开的合成参考数据上离线训练）不含租户数据，按平台资产标记（`organization_id = null`）。

## 5. 权限设计

- **唯一入口：** `app/services/permissions.py` 中的 `PERMISSIONS`（动作 → 角色）加上组织范围与资源归属检查。所有服务都调用它，接口不自行判断。
- **跨组织访问：** 越界 id 统一返回 404，不泄露对象是否存在；每个 403 都记为 `PERMISSION_DENIED` 安全事件。
- **角色矩阵：**
  - admin：全部组织；组织、用户、审计授权、配置写入、风险规则写入、运维指标。
  - auditor：被授权的组织；模型激活、回滚与退役；业务结果录入与纠正；审计复核；配置与安全事件只读。
  - risk_manager：本机构的预警、任务、驾驶舱与规则（只读）。
  - financier：本机构的申请与融资操作、驾驶舱、模型只读。
  - supplier / core_enterprise：仅涉及本企业的申请与融资。
- **测试覆盖：** `tests/test_release_security.py` 对 6 个角色 × 11 个接口逐一断言可访问矩阵，并校验写操作只归属单一角色（例如模型激活只允许审计员，管理员也不行）。

## 6. 部署方式

- **容器编排：** 单机 Docker Compose（PostgreSQL 17 + 应用），两个服务都设置 `restart: unless-stopped`；健康检查指向 `/api/v1/ops/health`。
- **配置与密钥：** 所有配置来自环境变量或 `.env`；`scripts/ops/init-env.sh` / `.ps1` 生成随机密钥。
- **演示数据：** `DAIBM_DEMO_DATASET=true` 时首次启动生成演示数据集，约 30 秒。
- **备份恢复：** 使用 `scripts/ops/backup.sh` 和 `restore.sh`，恢复前后都做哈希校验。
- **文档：**
  - 部署：`DEPLOYMENT_GUIDE.md`（环境要求、配置、启动、初始化、备份、恢复、v1 升级）；
  - 操作：`USER_GUIDE.md`（按角色说明）；
  - 发布：`RELEASE_NOTES.md`、`CHANGELOG.md`；版本号记录在 `VERSION`，API 读取同一版本。

## 7. 测试结果

| 检查 | 结果 |
|---|---|
| `ruff check .` | All checks passed |
| `mypy app` | Success: no issues found in 90 source files |
| 全量测试 `pytest tests --ignore=tests/research` | **922 passed, 12 skipped, 0 failed**（Phase 4 结束时为 899 passed） |
| Phase 5 新增测试 | `test_tenant_isolation_completion.py` 11 项；`test_demo_dataset.py` 3 项；`test_release_security.py` 9 项 |
| CI | 推送后在 PR #13 上运行（结果见 PR） |

**Docker 验收**（v1.0.0 镜像）：

| 项目 | 结果 |
|---|---|
| 1. clean start | 新 Compose 项目、空数据卷：33 秒 healthy。演示数据集已生成：39 笔已结清和 1 笔逾期融资，ACTIVE v1 与 CANDIDATE v2，2 个快照；55 条历史预警已关闭，1 条处理中、4 条待处理；1 个进行中的任务 |
| 2. existing data upgrade | Phase 4 数据库（0020，60 笔融资、56 条结果、ACTIVE 模型）升级到 0021 后 healthy。融资、结果、账本数量不变，模型状态不变（RETIRED, RETIRED, ACTIVE），转移次数不变（11），账本链头哈希不变；所有申请都有放款机构；API 版本为 1.0.0 |
| 3. backup | `backup.sh v1-drill`：52 个文件，校验通过 |
| 4. restore | 备份后新建的组织在恢复后消失；恢复前后的哈希校验都通过，`ledger_valid=true`，链头哈希与备份一致，恢复后 healthy |
| 5. crash restart | SIGKILL uvicorn 后自动重启（RestartCount 0→1），恢复 healthy；崩溃前的会话仍有效；两个 worker 心跳均恢复 |

**演示全流程**（`scripts/v1_acceptance.py`，通过 HTTP、以各自角色执行）：

1. **申请：** 企业申请提交给 BANK-001，核心企业确认交易。
2. **风险评估与模型决策：** 原始分 0.3355，经 `calibration:controlled_demo@v1` 校准为 0.3084；决策血缘指向 32 条训练结果的快照。
3. **审批：** 批准，完成控制措施与审计复核。
4. **放款：** 放款完成。
5. **还款：** 第二期还款确认。
6. **逾期：** 第一期标记逾期。
7. **预警：** OVERDUE_DAYS 预警被指派并进入处理。
8. **任务处理：**
   - 催收任务依次完成开始、备注、结果文件和完成；
   - 第一期还清后融资转为 repaid；
   - 预警解决后，由审计员审核关闭。
9. **结果反馈：** 融资结清后录入业务结果。
10. **模型治理：** worker 基于 BANK-001 的新快照训练出 `@v3`，经 DRAFT → EVALUATING → CANDIDATE → ACTIVE 自动激活；账本校验通过。

浏览器验证：风险经理、审计员、供应商的主要页面均无前端错误；供应商表单提供放款机构选择。

## 8. 性能基线

1000 笔融资 / 5000 条业务结果 / 10000 条审计事件（详见 `docs/performance/PERF_BASELINE.md`）：

| 操作 | 中位数 | SQL 语句数 |
|---|---:|---:|
| 风险驾驶舱 | 121–155 ms | 16 |
| 风险详情 | 34 ms | 37 |
| 预警扫描（稳态 / 首次产生 50 条预警） | 19 ms / 333 ms | 9 / 359 |
| 决策时模型查找（含工件校验） | 61 ms | 1 |
| 模型注册表列表 | 6 ms | 4 |
| 业务结果列表（200 行） | 47 ms | 1 |
| 训练资格预览（全部 5000 条） | 637 ms | 2 |

明显瓶颈：
- 训练资格预览随结果数线性增长；
- 决策时每次重新校验模型工件；
- 首次扫描逐条写入预警；
- 驾驶舱在应用层汇总。

所有操作都在 1 秒以内。按冻结架构的原则，本版本不做优化。

## 9. 已知限制

1. **模拟边界**：放款与还款是受控模拟，不连接真实银行、支付或会计系统；演示与性能数据为合成数据。
2. **训练规模**：每个组织需要至少 30 条有效结果（训练 20 + 验证 10，两类样本齐全）才能训练模型；
   结果较少的机构只使用透明基线评分。
3. **历史数据归属**：迁移时，历史训练数据如果混合了多个组织，或存在多个放款机构且申请从未被处理，迁移会拒绝执行，需要人工处理。
4. **单实例**：请求指标和心跳保存在进程内；配置有 5 秒缓存；登录锁定按账号计数，IP 限流需在反向代理上配置；多副本部署需要按实例抓取指标。
5. **备份**：采用逻辑备份（COPY），适用于当前数据规模；GB 级以上数据应改用 `pg_dump` 或 WAL 归档。
6. **性能**：见第 8 节的瓶颈，未做优化。
7. **研究核心**：研究核心是平台级的合成参考模型，不参与租户的信贷决策校准。
8. **可选模块**：Fabric 只锚定哈希；ZKP 由应用信任内部 prover，不在 Python 中独立验证 Groth16。
9. **首个生产管理员**：没有独立的初始化命令，步骤见部署指南。

## 10. Release hash

| 提交 | 内容 |
|---|---|
| `be79600` | Phase 5.1/5.2：模型、快照与申请的租户隔离；演示数据集 |
| 见 PR #13 最新提交 | Phase 5.3–5.6：性能基线、安全检查、发布文档、验收脚本与本报告（v1.0.0 release） |

分支：`claude/funny-ptolemy-32pkcy`（PR #13，未合并，等待确认）。

# DAIBM-SCF 用户指南 / User guide (v1.0.0)

界面默认俄语，右上角 `中文` 切换中文。所有演示账号的密码由部署配置 `DAIBM_DEMO_PASSWORD` 设定（见 `.env`）。
每个用户只能看到本组织的数据；越权访问会被拒绝并记录为安全事件。

## 角色与账号

| 角色 | 演示账号 | 组织 | 能做什么 |
|---|---|---|---|
| 企业用户（供应商） | `supplier.demo`；演示企业 `supplier.normal.demo`、`supplier.watch.demo`、`supplier.default.demo` | 各自的供应商企业 | 创建、修改、提交融资申请（选择核心企业与放款机构），提交还款 |
| 企业用户（核心企业） | `core.demo` | 核心企业 | 确认或退回交易，填写认可的应付上限 |
| 融资方 | `financier.demo` | 放款机构 BANK-001 | 风险评估、融资决策、放款、确认还款、标记逾期 |
| 风险经理 risk_manager | `risk.demo` | BANK-001 | 风险驾驶舱、预警处理、风险任务、违约处置 |
| 审计员 auditor | `auditor.demo` | 审计机构（仅能看到被授权的放款机构） | 审计复核、业务结果录入与纠正、模型注册与激活、审计查询 |
| 管理员 admin | `admin.demo` | BANK-001 | 查看全部组织、组织与用户管理、审计授权、配置中心、风险规则、运维监控 |

## 1. 风险查看（risk_manager / 融资方 / 审计员 / admin）

1. 登录后打开 **风险驾驶舱**：资产（放款总额、余额、企业数、笔数）、风险分布（正常/关注/高风险/违约）、
   生命周期统计、损失与回收、模型状态（当前 ACTIVE 模型、最近快照、最近失败原因）、预警与任务汇总。
2. 在列表中点开一笔融资进入 **风险详情**：企业信息、融资与还款、风险评分与历史、使用的模型版本及工件哈希、
   生命周期时间线与审计记录。
3. 演示数据中：“东方物流”（逾期 35 天）处于关注，“西部化工”已违约并核销，“稳健制造”正常结清。

## 2. 预警处理（risk_manager 处理，审计员/admin 复核）

1. 打开 **风险预警中心**，按状态/严重度筛选。规则每 60 秒自动扫描，也可点击“运行规则检测”。
2. 预警流程：`OPEN → 指派 → 处理中 → 已解决 → 审核关闭`（审核人不能是处理人；审计员可以退回重新处理）。
3. 在预警上 **创建任务**（催收、调查、处置复核等），指定负责人与截止时间。
4. 在 **风险任务中心** 查看“我的任务”，开始处理、添加备注、上传结果文件、完成或取消。演示中“东方物流”的
   预警处于处理中，并已有一项进行中的催收任务。
5. 风险规则（阈值、严重度、启用）在 **风险规则中心** 由管理员维护，每次修改生成新版本，可回滚。

## 3. 模型查看（融资方 / risk_manager / 审计员 / admin）

1. 打开 **模型中心**：每个组织、每个范围（controlled_demo / external_verified）各有一个 ACTIVE 模型；
   列表显示版本、状态、训练样本数、验证前后 Brier、工件哈希、创建与激活时间，以及所属组织。
2. **CANDIDATE** 版本已通过独立的时间外验证，等待审计员人工激活（演示中有一个）。审计员可激活、回滚到上一版本或退役。
3. **数据快照** 页面显示每次训练读取的精确数据集（包含/排除的业务结果及原因），并链接到使用它的模型版本。
4. 在申请详情的“风险决策”中可查看每次评分的 **决策血缘**：决策 → 模型版本 → 数据快照 → 训练结果。

## 4. 审计查询（审计员 / admin）

1. **审计账本**：所有业务事件按哈希链记录，可一键校验完整性；篡改会被检测并可按事件恢复。
2. **业务结果治理**：结果复核队列、训练资格预览（每条结果为何被纳入或排除）、纠正与替代修订的历史。
3. **平台管理 → 安全事件**：登录成功/失败、账号锁定、会话过期、越权访问（403）和管理员操作。
4. **平台管理 → 配置中心**（审计员只读）：每个配置项的版本、修改人、原因与回滚记录。
5. 审计员只能看到被授予审计权限的组织；授权由管理员在“组织与用户 → 审计授权”中管理。

## 5. 企业用户的申请流程

1. 供应商：在 **业务工作台** 填写申请（核心企业、放款机构、合同与发票号、金额、期限及风险因素），保存草稿并提交。
   提交后放款机构不可更改。
2. 核心企业：确认交易并填写认可的应付上限，或退回修改。
3. 放款机构依次完成风险评估、决策，风险经理设置控制措施，审计员复核；随后在 **融资生命周期** 页面放款。
4. 供应商在融资页面提交还款，融资方确认；逾期、处置、违约、追偿、核销与结清都有完整记录。

## 常见问题

- **看不到某笔申请/融资？** 它属于其他组织，或者尚未进入你的角色负责的阶段。
- **登录被拒绝（423）？** 连续失败次数过多，账号被临时锁定；等待锁定时间结束，或请管理员停用后再启用该账号。
- **会话过期？** 空闲 30 分钟或登录超过 12 小时后需要重新登录（可在配置中心调整）。

# Engineering closeout checkpoint — 2026-09-23

This is a dated, sanitized engineering record for the local DAIBM-SCF MVP. It does not amend the thesis, claim production deployment, or substitute for a current health check of the original `127.0.0.1:8010` service. Private database rows, credentials, identities, archives, and restore evidence remain local and are not in this repository.

## Code and automated verification

- Commit `2980a1857737976ca55aa0ce52135dc4f02cb430` aligns the application with the gateway's explicit `500 / INTERNAL_ERROR / retryable=true` contract. Only this exact, well-formed JSON response receives automatic retry, limited to three total sends using the existing outbox backoff; `503`/`504`, permanent failures, leases, and anchor IDs retain their existing behavior. Malformed, oversized, or non-JSON 500 responses fail closed. A gateway test covers a Fabric commit followed by a lost 500 response: retry reads the existing anchor without a second submit.
- On an isolated Testcontainers PostgreSQL database, `RUN_ZKP_WORKFLOW_INTEGRATION=1 python -m pytest tests --ignore=tests/research -o addopts='' -q -ra --durations=10` completed with **753 passed, 1 skipped, 0 failed, 0 xfail** in **945.66 s**. The real ZKP HTTP/workflow integration ran. The single skipped maintenance-backup case requires a separate explicit Docker cold-backup opt-in; it was not pointed at the original database.
- The full gateway suite ran in the pinned Linux Node container with `npm ci --no-audit --no-fund && npm test`: **14 passed, 0 failed, 0 skipped**. `python -m ruff check .` passed; `python -m mypy` reported no issues in 63 source files. `git diff --check` passed before the code commit. Windows-only execution of the full gateway suite is not counted as a pass because two tests require the documented Linux `/work` container layout.
- The PR-triggered CI run [35811228781](https://github.com/MengdanXue/daibm-scf-mvp/actions/runs/35811228781) is completed/success for that exact code SHA: `application-tests`, `research-tests`, `advanced-tests`, and `static-analysis` all passed. CI is evidence about the submitted code, not about the local 8010 runtime.

## Docker and original environment at this checkpoint

- Normal Docker Desktop startup initially failed on stale socket endpoints. The small runtime socket directories were preserved locally before a normal restart; there was no factory reset, prune, WSL shutdown, image/volume deletion, or other-project service stop. Docker Engine 29.7.2 subsequently became reachable.
- The original inventory contained 64 stopped containers, 46 volumes, 17 networks, and 11 Compose projects. All 64 had restart policy `no`; no other project auto-started. The original app, PostgreSQL, peer, orderer, CLI, gateway, and prover remain stopped. Port 8010 has no listener. **Docker Engine running is not original 8010 health.**
- The original persisted volume names, application image, and 8010 loopback binding matched the prior plan. The sole source container-ID difference was the peer-managed, mount-free chaincode runtime already recorded in the 2026-09-20 handoff; its image was unchanged. This did not authorize a ledger rewrite.

## Fresh backup and isolated recovery, not original startup

- After exact-code CI passed, a new private 2026-09-23 plan pinned 9 stopped source containers and 6 persistent stores. A coordinated cold backup completed at **2026-09-23 10:45:59 Asia/Shanghai** with unchanged pre/post source manifests and archive-by-file content/metadata verification. All 10 private backup files were independently rehashed; the completion manifest SHA-256 is `a90efabd924e05f33ec6f2e44df956e16252dc4b88888d3ab48f88cf9aee4d7c`.
- Restoration used five **new** clone volumes and a new Fabric bind directory, disjoint from original stores and the 2026-09-20 clone. All restored files and metadata matched the fresh backup before any clone service started. Only the clone PostgreSQL, orderer, peer, CLI, and the peer-managed chaincode runtime were started for readback; no clone app, gateway, prover, bootstrap, migration, seed, calibration worker, or outbox dispatch was run. The clone services were stopped after verification, with clone stores and evidence retained.
- The clone PostgreSQL was healthy and read-only snapshots reported Alembic revision `20260908_0014`, 29 tables, 316 rows including the revision row, 98 valid hash-chain events, 49 anchored and 49 pending outbox rows, and zero active calibration jobs. Per-primary-key/full-column row digests, full sequence state, revision, ledger head, and outbox identities matched the 2026-09-20 final database snapshot; no pending record was processed.
- Clone Fabric peer CLI readback reported channel height **54**, committed chaincode **version 1.0 / sequence 2**, and all **49** currently anchored outbox IDs. The 2026-09-20 chain snapshot sampled only 41 historical anchors, although its later database snapshot listed 49 anchored IDs. An initial exact-map comparison therefore stopped as designed and its failure record was preserved. The reviewed comparison verified those 41 historical raw anchor values and SHA-256 values individually, checked all 49 current anchored IDs against Fabric, and matched the recorded final height/definition. It passed with zero discrepancies. This is a corrected comparison scope, not a change to ledger data.
- Calibration artifacts and all non-ledger Fabric identity/configuration manifests matched the earlier backup file-by-file and metadata-by-metadata. The 2026-09-20 acceptance remains **historical executed evidence**; today's clone recovery is separate and does not make the original 8010 currently healthy.

## Open boundary and next authorized gate

The original 8010 PostgreSQL, Fabric, and app were **not** started. Their current live SQL/peer/API health, five-role smoke flow, and a real-original-environment HTTP 500 recovery are **not completed** on 2026-09-23. The new automated 500 tests establish the bounded policy and idempotent gateway path, but do not count as an original Fabric block-count acceptance. The app's normal command executes `alembic upgrade head`; startup also invokes identity seeding/reconciliation and enables a calibration worker. Starting the original app under the current read-only/clone-only authorization would violate that boundary. A separately reviewed startup/deployment plan and approval are required before claiming current original-8010 acceptance or merging PR #9 on that basis. Historical pending business records must remain untouched.

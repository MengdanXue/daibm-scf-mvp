# 原 8010 冷备份恢复与隔离克隆验收（2026-09-14）

## 本阶段结论

本轮从原 8010 冻结环境重新生成冷备份，恢复到全新隔离克隆，
完成数据库迁移、既有 Fabric 合约升级、受控业务验收和应用重启后持久性检查。
原始数据与链上历史保留验证通过；这不是复用 9 月 8 日的验收环境或备份。

截至本阶段结束，原部署的 9 个钉住身份的容器仍处于停机冻结状态。
原 8010 尚未执行数据库迁移、链码升级或应用启动；正式原环境升级属于下一阶段。
本报告仅记录本地开发网络的工程验收，不构成生产可用性或论文性能结果。

## 代码与验证门槛

- 功能目标基线：`27e56fd`（PR 8）。
- 维护脚本远端提交：`3b6f299`；本地提交 `56b3629` 与其完整代码树一致。
- 维护变更：[PR 9](https://github.com/MengdanXue/daibm-scf-mvp/pull/9)。
- 精确提交 CI：[34769607614](https://github.com/MengdanXue/daibm-scf-mvp/actions/runs/34769607614)，
  application-tests、research-tests、advanced-tests、static-analysis 均成功。
- 本地门槛：759 个 Python 测试、22 个隔离 Fabric shell 用例通过；Ruff、应用 mypy 通过。
- 私有恢复脚本在实机执行前通过 16 个不依赖 Docker 的测试；失败目标保留且禁止覆盖。

| 组件 | 本轮克隆使用的版本或镜像 ID 前缀 |
| --- | --- |
| PostgreSQL | 17.11；`18cfe3ef5e68`，与源数据库镜像完全相同 |
| 应用 | `8325d9321f4b` |
| Fabric gateway | `d7009e762f35` |
| 真实证明服务 | `cdcb633d6c03`；电路 `invoice_limit@1` |
| Fabric peer / orderer / CLI | 2.5.16；分别为 `09ee75042de9` / `e322c57331d3` / `1d163b251345` |

实际执行使用完整镜像 ID；上表仅公开便于识别的缩写。

## 新备份及恢复证据

北京时间 00:52:36 完成协调冷备份，00:57:02 完成隔离物理恢复。
六份归档分别覆盖数据库、校准工件、Fabric 数据及三份 Fabric 配置存储。
源在整个备份期间保持冻结；全部源文件清单前后相等，所有归档内容与源清单相等。

五份 Linux 卷恢复到五个新命名卷；原 Windows 绑定存储恢复到独立的新绑定目录。
恢复前统一拒绝既有目标和源目标重叠；恢复过程未挂载源存储、未启动任何业务服务。
首次启动克隆前，逐项比较全部 1,764 条清单记录，包含根目录、类型、内容哈希、
UID/GID、权限、整秒 mtime 和符号链接目标；六个目标全部相等。
备份完成清单的 SHA-256、各归档哈希及恢复完成记录形成可核对的私有证据链。

源数据库控制文件显示先前关闭不干净。恢复完整保留 WAL，先仅启动克隆 PostgreSQL。
17.11 跳过初始化，正常重放 WAL 并完成恢复检查点，随后就绪；系统标识与恢复前一致。
应用尚未启动时，已采集数据库基线、完整逻辑导出和模式导出。

## 历史基线、迁移与合约升级

| 项目 | 新恢复克隆的实读基线 | 升级与复验结果 |
| --- | --- | --- |
| 数据库 revision | `20260824_0010` | 迁移至 `20260908_0014` |
| 原始表与行 | 22 张 public 表；排除 Alembic 版本表后 136 行 | 原列投影下全部历史行保留 |
| 序列 | 3 个序列的完整定义及状态 | 迁移期间全部相等 |
| 数据库审计链 | 33 个有效事件 | 迁移及应用首次启动后头哈希不变 |
| 原有 outbox | 33 项，全部 anchored | 对应 33 条链上原始记录逐字节不变 |
| Fabric 合约 | `audit-anchor`，version `1.0`，sequence 1 | version 仍为 `1.0`，sequence 升至 2 |
| Fabric 高度 | 36 | 升级后 38；重复部署后仍为 38 |

33 个历史 anchor 的集合来自本次恢复数据库的完整查询，不依赖分页 API。
9 月 8 日归档仅作为额外交叉参考；升级比较使用本轮新捕获的原始链上记录。
严格既有 channel 模式未执行 bootstrap、重建身份或重新建链。
目标 package 为 `audit-anchor_1:850fdf6d8ae02d0b4d6b14a1bd3728273b9f6eadf358e224df7c530f9219c491`。

重复数据库迁移后，除采集时间等证据封装字段外，完整快照内容相等。
重复链码部署识别已提交的精确 package；channel 高度与区块哈希、批准/提交定义、
33 条历史原始记录均不变。迁移 revision 单独校验，不以历史行比较代替版本检查。

## 受控业务、故障恢复与重启

克隆使用独立项目、网络和存储，界面仅绑定本机 8030。
受控五角色业务流程产生 7 个新审计事件；真实证明验证通过，篡改公开信号被拒绝。
持久化证明 SHA-256 为 `bcdd278df7782b72f083de247bf49737f2e371968d224e37a9836ab65cf206cc`。
合成证明事件哈希为 `a98d364b768f9d4fee8cf736c6dcd746b33c0917d481cd4537e8f3d6958adeab`。
7 项 dispatch 全部 anchored，高度 38 → 45；gateway 与 peer CLI 读回相等。
完整电路版本 `invoice_limit@1` 与同一证明哈希在持久化事件、outbox 和链上记录中一致。
同内容重复提交返回 200，新增区块数为 0；移动端锚定界面核验通过。

随后对克隆 gateway 执行一次受控停止：唯一待发送项保留为可重试，错误为
`FABRIC_UNAVAILABLE`；gateway 恢复后同一项成功 anchored，attempt_count 为 2。
故障恢复合成事件哈希为 `97052470f75e90acbb2491de29784718e7dcb6d351f762da5b8071faa9372d63`。
双路径读回相等，重复提交返回 200；高度 45 → 46，gateway 已恢复。

验收共新增两个合成申请。克隆最终为 8 个申请、41 个有效审计事件、41 个 outbox 项。
验收后的历史比较允许这些新增行和必要的序列推进，但仍逐项要求原始 136 行不变；
原有 33 条链上原始记录继续保持逐字节一致。
应用重启后重新采集的完整数据库快照与验收后快照相等，仅采集元数据不同，
revision、序列、41 个事件及头哈希均保留，应用恢复健康。

## 原环境下一阶段与证据边界

本阶段关闭“本轮真实备份能够恢复，并在隔离克隆通过升级及受控验收”的门槛。
原 8010 仍需按其身份、挂载、数据库及链上历史重新核对，执行正式升级并独立验收。
本报告不把克隆成功写成原部署已经升级，也不声称 Docker 复发问题已永久根治。
未验收 peer/orderer 故障、网络分区、多故障场景、跨组织容错或生产隔离。
备份、完整镜像钉住信息、逐行证据和运行记录保留在私有证据集中；
公开报告不包含本机路径、密钥、凭据、会话行或原用户标识。

# 原 8010 升级与独立受控验收（2026-09-14）

## 本轮结论与范围

原部署已完成数据库迁移、既有 Fabric 合约升级和应用替换，并在本机
`127.0.0.1:8010` 独立通过五角色流程、真实证明、锚定幂等性及 gateway 故障恢复验收。
北京时间 2026-09-14 02:25 的部署核查确认应用及 PostgreSQL 健康，原 gateway 已恢复运行。
原数据库的 136 条历史行和 33 条原始链上记录均保留；验收新增两个明确标记的合成申请。

此前的 [8030 隔离克隆报告](original-8010-clone-recovery-2026-09-14.md)
记录冷备份恢复、克隆升级和克隆应用重启检查，是本轮原部署升级的前置验证。
本报告使用随后单独采集的原 8010 数据库、链上及业务证据；下文的事件哈希和证明哈希
均属于原部署本轮验收，不复用克隆结果。

## 代码来源、镜像与已完成的 CI

功能目标为已合并 main `27e56fdcfa76e4613789dd67323b797473b15c1f`。
维护脚本版本为远端 `3b6f299b899486351fb86b1fa8f7c46e235a3f5c`；
阶段一克隆报告版本为 `2d499afbfaa3b947717d44ec83b7a0efacae9631`。
本轮使用同一组已在新克隆验收的完整镜像 ID：

| 组件 | 实际运行的镜像 ID |
| --- | --- |
| 应用 | `sha256:8325d9321f4bd35eb3c47b352987559450f87f2e1b7f1a7297ff5b8225983c04` |
| Fabric gateway | `sha256:d7009e762f35ee6eb398baad7c44cd9674316b4e7c1dfbcf3b2ec4c666fd9746` |
| 真实证明服务 | `sha256:cdcb633d6c03dde9a979d952c62a1b15ff5b45f08dd8fff2d94f701ce1fc4185` |

镜像内 Dockerfile 复制的功能源码和工件已与上述 main 功能基线核对。
具体文件范围和换行归一化规则见
[维护准备报告](original-8010-maintenance-scripts-2026-09-14.md#runtime-image-provenance)；
该文件核对不代表对基础镜像及全部已安装依赖的逐文件验证。
原运行状态记录再次确认了实际镜像 ID、8010 回环绑定及 `ZKP_PROOF_REQUIRED=true`。

| 已发布提交 | 已完成的 CI |
| --- | --- |
| 维护代码 `3b6f299` | [PR run 34769607614](https://github.com/MengdanXue/daibm-scf-mvp/actions/runs/34769607614) 成功 |
| 阶段一报告 `2d499af` | [push run 34773869629](https://github.com/MengdanXue/daibm-scf-mvp/actions/runs/34773869629) 与 [PR run 34773872920](https://github.com/MengdanXue/daibm-scf-mvp/actions/runs/34773872920) 均成功 |

上述每条 CI 的 application-tests、research-tests、advanced-tests、static-analysis
四项检查均已完成并成功。它们对应各自明确的提交，不替代本报告提交后的检查。

## 原数据基线与数据库迁移

原业务写入保持停止时，先启动原 PostgreSQL、peer、orderer 和 CLI 并采集基线。
原 PostgreSQL 的完整历史行、原列及序列状态与本轮新恢复的克隆基线相等，
数据库系统标识也一致；另行保留了原库恢复后的逻辑导出和模式导出。
原链的 channel、提交/批准定义及 33 条原始记录也与克隆升级前基线一致。
这些核对完成后才进行原库迁移及原合约升级。

| 核对对象 | 原部署升级前 | 原部署升级、首次启动及最终验收结果 |
| --- | --- | --- |
| 数据库 revision | `20260824_0010` | `20260908_0014` |
| 历史数据 | 22 张 public 表；排除 Alembic 版本行后 136 行 | 按每个原主键、全部原始列逐项保留 |
| 序列 | 3 个序列的完整定义、`last_value`、`is_called` | 迁移及首次应用启动后相等；合成验收后允许正常推进 |
| 申请 | 6 个历史申请 | 8 个：原 6 个及本轮 2 个合成申请 |
| 数据库审计链 | 33 个有效事件 | 41 个有效事件；全部原事件不变 |
| outbox | 33 项，全部 `anchored` | 41 项，完整查询确认全部 `anchored` |
| 原始 Fabric 记录 | 33 条 | 升级、重复部署及最终验收后逐字节一致 |

迁移通过独立 Alembic 入口执行，核对时应用尚未启动。
原列投影中所有历史行和三个序列保持一致，revision 单独核对为目标版本。
随后再次执行迁移，29 张表的完整列、行、序列、revision 和审计链内容均相等；
快照采集时间等封装元数据不参与此无操作判定。

应用首次启动后的独立快照仍保留全部 136 条原行，三个序列及 33 事件的头哈希未变。
最终业务验收允许新增合成行及序列推进，同时继续逐主键核验原始 136 行；
旧行被修改或删除仍会判失败。迁移、重复迁移、启动及验收比较均通过，
对应比较记录引用的输入文件 SHA-256 也已重新核对。

## 原合约升级与部署保留情况

在现有 `scfchannel` 上执行严格既有 channel 模式，`audit-anchor` 的 version
保持 `1.0`，sequence 从 1 升至 2。目标 package 为：

`audit-anchor_1:850fdf6d8ae02d0b4d6b14a1bd3728273b9f6eadf358e224df7c530f9219c491`

批准和提交定义使高度从 36 增至 38。再次部署识别已提交的精确 package，
高度、当前/前一区块哈希、批准/提交定义及 33 条原始记录均不变。
原记录的完整原始字符串逐项比较，不通过重新格式化时间戳掩盖差异。

原 PostgreSQL、peer、orderer 保留各自原容器 ID。
应用、CLI、gateway、prover 按目标代码和镜像进行必要替换；原数据库卷、校准工件卷、
Fabric 账本、身份及配置存储保持原映射。最终运行清单确认六份受保护源存储仍在使用，
bootstrap 仍停止，运行实例属于原部署和本轮克隆，未发现意外启动的其他项目。
未重建身份、重新创建 channel 或以克隆存储替换原存储；旧卷和私有备份继续保留。
验收后的只读核对确认，87 个身份、配置及其他非 state 文件的内容和相应 174 条
文件/目录元数据记录与本轮冷备份一致，新增、缺失、内容或元数据差异均为 0；
该核对在遍历前排除了持续运行中的 state 树。

## 原五角色流程与真实证明

本轮原 8010 的供应商、核心企业、融资方、风险管理和审计五角色浏览器流程通过。
持久化的真实 `invoice_limit@1` Groth16 证明被独立验证，修改后的公开信号被拒绝；
同一完整电路版本和证明哈希在事件、outbox 与 Fabric 记录中一致。

- 原部署合成证明事件哈希：`ed8a1a2a99d63fe0dba5795972188df5a838999d54d4ba71c6363ba32e5ab97c`。
- 持久化证明 SHA-256：`ddf941d5b0f92dea6d60da81e9ab6ba693221a7a9a8cc564d353558473f743c1`。

北京时间 02:19:56 完成的原部署证明验收中，7 个新锚定项全部成功，
无 retryable 或 permanent failure，下一次 dispatch 领取数为 0；链高 38 → 45。
gateway 与独立 peer CLI 读回一致，同内容重复 POST 返回 200 且新增区块数为 0。
移动端锚定界面核验通过。五角色及锚定界面截图保留在本机私有证据集中。

## 原 gateway 故障恢复与最终状态

随后创建一个明确标记为 `SIM-OUTAGE` 的合成草稿，对身份已固定的原 gateway
执行一次受控停止。故障期间唯一待发送项保持可重试，错误为 `FABRIC_UNAVAILABLE`，
审计链仍有效，链高保持 45。脚本的 `finally` 路径启动并验证同一个 gateway；
恢复后同一 anchor 在第 2 次尝试成功，未创建另一个替代 anchor。

原部署故障恢复事件哈希：
`f51016210813ddada5e3c1fb25043832d4fdaf73bb6a456c183b1729e0e39a97`。

北京时间 02:21:19 的结果确认 gateway/CLI 读回一致、重复 POST 返回 200，
恢复提交使高度从 45 增至 46，重复提交未再增加区块，下一次 dispatch 领取数为 0。
最终原库为 8 个申请、41 个有效审计事件和 41 个全部 `anchored` 的 outbox 项。
原有 136 行及 33 条原始链上记录再次通过保留检查，合约保持 sequence 2、version `1.0`。

## 验收边界与报告发布

本轮原部署完成了上述升级和受控功能验收；应用替换后的首次启动已验证，
但没有再执行一次原应用重启。应用重启后的持久性检查属于前述克隆阶段，
不作为本轮原应用再次重启的证据。

本轮未注入 peer/orderer 故障、网络分区或多点故障，未验证跨组织容错或生产隔离；
没有进行生产部署或公网发布，也不据此声称 Docker 启动故障永不复发。
新增数据均为受控合成验收记录，不代表真实融资、转账或论文模型性能结果。
公开报告不包含密钥、凭据、私有备份、会话行、原用户标识或本机私有路径；
完整逐行、原始链上及挂载证据与截图保持私有，未随报告上传远端。

本报告提交后的最终 CI 状态，以
[PR 9 当前提交的 Checks](https://github.com/MengdanXue/daibm-scf-mvp/pull/9/checks)
为准；本报告不预先指定其自身未来提交 SHA，也不将先前提交的成功冒充该提交的检查结果。
